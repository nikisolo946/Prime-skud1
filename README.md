# ПРАЙМ СКУД — Веб-страница для открытия ворот

## 📋 Описание задачи

Разработать веб-страницу для размещения на бесплатном хостинге (GitHub Pages), которая позволяет удалённо открывать ворота через систему **ПРАЙМ СКУД** без перезагрузок страниц, всплывающих окон и лишних действий.

### Исходные данные

- Функционал открытия ворот размещён в облаке ПРАЙМ ПУЛЬТ
- Доступ предоставляется по индивидуальной ссылке вида:
  ```
  https://prime-skud.ru/a/1055042-89ce8100d1c9fce82082a5c481a5f99c
  ```
- Страница содержит кнопки для открытия различных устройств (ворота, подъезды, калитки)

## 🔍 Анализ работы оригинальной системы

### Процесс активации ссылки

1. При первом открытии ссылки пользователь видит экран:
   ```
   Данная ссылка ещё не активирована.
   Одну ссылку доступа можно активировать только на одном устройстве.
   ```
2. После нажатия "Активировать...", сервер привязывает устройство через **куки браузера** (заголовок `Set-Cookie`).
3. При последующих посещениях сервер проверяет наличие этой куки.

### Техническая проблема
Современные браузеры жестко режут кросс-доменные запросы политиками `CORS` и `SameSite Cookies`.
Отправить `AJAX` (`fetch`) запрос с `github.io` на `prime-skud.ru` вместе с куками напрямую — **невозможно**, браузер заблокирует такой запрос. А использование `iframe` блокируется самим сервером ПРАЙМ СКУД (`X-Frame-Options: DENY`).

---

## 🚀 Финальное решение: Умный Прокси (Cloudflare Workers)

Чтобы обойти все ограничения безопасности браузеров, мы написали промежуточный прокси-сервер (Worker), который берет на себя общение с основным сервером и "ручное" проксирование куки.

### Как это работает "под капотом":

1. **Обход CORS**: Наш Worker всегда отвечает браузеру `Access-Control-Allow-Origin: *`, разрешая любые запросы с GitHub Pages.
2. **Получение куки (Активация)**:
   - Фронтенд шлет команду "Активировать" на Worker.
   - Worker отправляет реальный POST-запрос на `prime-skud.ru`.
   - `prime-skud.ru` отвечает успешной активацией и присылает заголовок `Set-Cookie`.
   - Worker перехватывает его и отдает фронтенду не как куку (которую браузер отвергнет из-за чужого домена), а как обычный заголовок `X-Set-Prime-Cookie`.
   - Фронтенд сохраняет полученный токен надежно в `localStorage`.
3. **Открытие ворот**:
   - При нажатии на кнопку "Ворота 1", фронтенд достает токен из `localStorage` и прикрепляет его в кастомный заголовок `X-Prime-Cookie`.
   - Worker перехватывает этот заголовок и подставляет его как **настоящую** куку (`Cookie: ...`) при отправке POST-запроса на открытие ворот.
   - Сервер Prime Skud видит валидную куку и открывает ворота. 

### Преимущества этого архитектурного решения:
✅ **100% Seamless UX** — ворота открываются моментально в фоне по кнопке "Открыть". Никаких перезагрузок, вкладок и белых экранов.
✅ **Полный обход CORS**.
✅ **Единый прокси** — Worker полностью универсален и не привязан к одной конкретной ссылке или токену (ссылка передается через заголовок `X-Target-Url`).
✅ **Бесплатность на 100%** (и Cloudflare Workers, и GitHub Pages бесплатны для небольших проектов).

---

## 📁 Структура проекта

```

├── index.html          # Красивый фронтенд с логикой background fetch
├── worker.js           # Код прокси для развертывания в Cloudflare Workers
└── README.md           # Этот файл
```

## 🛠 Запуск и деплой

### Шаг 1: Развертывание Cloudflare Worker (Прокси)
1. Зарегистрируйтесь на `dash.cloudflare.com` -> Workers & Pages.
2. Создайте новый Worker.
3. Скопируйте содержимое файла `worker.js` из этого проекта и вставьте в редактор кода в Cloudflare.
4. Нажмите **Deploy**. Скопируйте URL вашего воркера (вида `https://gate-open.your-name.workers.dev/`).

### Шаг 2: Развертывание Frontend (GitHub Pages)
1. Откройте `index.html` локально.
2. В самом начале скрипта (тег `<script>`) найдите `const WORKER_URL`.
3. Замените на URL, скопированный из Cloudflare.
4. Выложите `index.html` в репозиторий GitHub.
5. Зайдите в Settings -> Pages -> и включите Github Pages для ветки `main`.

---

## 📱 Интерфейс и удобство

Приложение построено как **PWA (Progressive Web App)** с адаптивным дизайном.

- Можно добавить приложение из браузера на главный экран телефона с помощью функции "На экран Домой". Оно появится в виде иконки как обычное приложение.
- Кнопки имеют обратную связь: показывают спиннер при ожидании `⏳` и зеленую галочку успеха `✅`.
- Если "сессия" истекает, приложение попросит ввести ссылку заново.

---

## 📞 Контакты

Сервис ворот физически предоставлен компанией **ПРАЙМ ПУЛЬТ**
- Сайт: https://prime-pult.ru/

---

## 📝 Лицензия

Данный проект является пользовательской разработкой для личного использования и обеспечения лучшего пользовательского опыта (UX).
🇷🇺 Сделано в России
