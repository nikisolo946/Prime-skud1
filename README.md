# ПРАЙМ СКУД — Веб-страница для открытия ворот

## 📋 Описание задачи

Разработать веб-страницу для размещения на бесплатном хостинге (GitHub Pages), которая позволяет удалённо открывать ворота через систему **ПРАЙМ СКУД**.

### Исходные данные

- Функционал открытия ворот размещён в облаке ПРАЙМ ПУЛЬТ
- Доступ предоставляется по индивидуальной ссылке вида:
  ```
  https://prime-skud.ru/a/1055042-89ce8100d1c9fce82082a5c481a5f99c
  ```
- Страница содержит кнопки для открытия различных устройств (ворота, подъезды, калитки)

### Пример оригинальной страницы

```html
<html>
<head>
  <title>ПРАЙМ СКУД: ТСЖ Паруса над Камой</title>
</head>
<body>
  Объект: ТСЖ Паруса над Камой
  Ссылка доступа: #1055042
  
  Ворота 1: (Online: сейчас)
  [Ворота 1] ← кнопка
  
  Ворота 2: (Online: сейчас)
  [Ворота 2] ← кнопка
  
  Ворота 3: (Online: сейчас)
  [Ворота 3] ← кнопка
  
  Подъезд К37: (Online: сейчас)
  [Подъезд К37] ← кнопка
</body>
</html>
```

---

## 🔍 Анализ работы оригинальной системы

### Процесс активации ссылки

1. При первом открытии ссылки пользователь видит экран:
   ```
   Данная ссылка ещё не активирована.
   Одну ссылку доступа можно активировать только на одном устройстве.
   
   [Активировать ссылку на этом устройстве]
   ```

2. После активации сервер привязывает устройство через **куки браузера**

3. При последующих посещениях открывается страница с кнопками открытия ворот

### Технические особенности

| Параметр | Значение |
|----------|----------|
| Метод запроса | `POST` |
| URL API | `https://prime-skud.ru/a/{token}` |
| Тело запроса | `d={deviceId}&r=1` |
| Кодировка | `windows-1251` |
| Куки | `bind_{linkId}={sessionHash}` |
| Content-Type | `application/x-www-form-urlencoded` |

### Заголовки запроса (оригинальная страница)

```
Cookie: bind_1055042=fcab1cafc1abcd0a8bda6e1df38c9f7e
Origin: https://prime-skud.ru
Referer: https://prime-skud.ru/a/1055042-...
Content-Type: application/x-www-form-urlencoded
```

---

## 🧪 Протестированные варианты решений

### Вариант 1: Прямой fetch с GitHub Pages

**Описание:** Отправка POST-запроса напрямую с страницы на GitHub Pages

```javascript
fetch('https://prime-skud.ru/a/{token}', {
  method: 'POST',
  headers: {'Content-Type': 'application/x-www-form-urlencoded'},
  body: 'd=1225&r=1'
})
```

**Результат:** ❌ **Не работает**

**Ошибка:**
```
Access to fetch at 'https://prime-skud.ru/...' from origin 'https://nikisolo946.github.io' 
has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present 
on the requested resource.
```

**Причина:** Сервер `prime-skud.ru` не отправляет заголовок `Access-Control-Allow-Origin` для запросов с чужих доменов.

---

### Вариант 2: Fetch с credentials: 'include'

**Описание:** Отправка запроса с куки браузера

```javascript
fetch('https://prime-skud.ru/a/{token}', {
  method: 'POST',
  credentials: 'include',
  headers: {'Content-Type': 'application/x-www-form-urlencoded'},
  body: 'd=1225&r=1'
})
```

**Результат:** ❌ **Не работает**

**Причины:**
1. CORS блокировка (как в Варианте 1)
2. Куки `prime-skud.ru` не доступны с домена `github.io`
3. Браузер не отправляет куки на другой домен

---

### Вариант 3: Cloudflare Workers (прокси)

**Описание:** Создание прокси-сервера на Cloudflare Workers

```javascript
// worker.js
export default {
  async fetch(request) {
    const API_URL = 'https://prime-skud.ru/a/{token}';
    const body = await request.text();
    
    const response = await fetch(API_URL, {
      method: 'POST',
      headers: {'Content-Type': 'application/x-www-form-urlencoded'},
      body: body,
    });
    
    return new Response(await response.text(), {
      status: response.status,
      headers: {'Access-Control-Allow-Origin': '*'}
    });
  }
};
```

**Результат:** ⚠️ **Частично работает**

**Проблема:**
- Worker отправляет запрос успешно (Status: 200)
- Но сервер `prime-skud.ru` требует куки сессии
- Worker не имеет доступа к кукам браузера пользователя
- Ворота не открываются

---

### Вариант 4: Форма с target="_blank"

**Описание:** Открытие оригинальной страницы в новой вкладке

```html
<form action="https://prime-skud.ru/a/{token}" method="POST" target="_blank">
  <input type="hidden" name="d" value="1225">
  <input type="hidden" name="r" value="1">
  <button type="submit">Ворота 1</button>
</form>
```

**Результат:** ⚠️ **Работает с ограничениями**

**Проблема:**
- Каждая новая вкладка = новое устройство для сервера
- Сообщение: "Ссылка уже была активирована на другом устройстве"
- Требуется активация для каждой новой вкладки

---

### Вариант 5: iframe для сохранения сессии

**Описание:** Загрузка `prime-skud.ru` в скрытом iframe для получения куки

```javascript
// Загружаем сессию
document.getElementById('sessionFrame').src = 'https://prime-skud.ru/a/{token}';

// Отправляем запрос через iframe
const form = document.createElement('form');
form.target = 'gateFrame';
form.submit();
```

**Результат:** ❌ **Не работает**

**Причина:**
- Сервер `prime-skud.ru` отправляет заголовок `X-Frame-Options: DENY`
- Браузер блокирует загрузку страницы в iframe
- Куки не сохраняются

---

### Вариант 6: navigator.sendBeacon

**Описание:** Использование Beacon API для отправки в фоне

```javascript
const data = new URLSearchParams();
data.append('d', '1225');
data.append('r', '1');
navigator.sendBeacon('https://prime-skud.ru/a/{token}', data);
```

**Результат:** ❌ **Не работает**

**Причина:**
- CORS блокировка
- Beacon не отправляет куки на другой домен

---

### Вариант 7: Переход в том же окне

**Описание:** Открытие `prime-skud.ru` в том же окне для сохранения куки

```javascript
window.location.href = 'https://prime-skud.ru/a/{token}';
```

**Результат:** ✅ **Работает!**

**Преимущества:**
- Куки сохраняются в браузере
- Нет CORS блокировки
- Ворота открываются корректно

**Недостатки:**
- Страница перезагружается
- Нужно возвращаться назад

---

## ✅ Итоговое решение

### Архитектура

Поскольку сервер `prime-skud.ru` требует:
1. **Куки сессии** (привязаны к домену `prime-skud.ru`)
2. **Same-Origin** запросы (CORS блокирует чужие домены)

**Единственное рабочее решение** — открывать оригинальную страницу `prime-skud.ru` для выполнения запросов.

### Реализованный функционал

Файл `index.html` содержит:

1. **Экран ввода ссылки**
   - Поле для вставки индивидуальной ссылки
   - Сохранение ссылки в `localStorage`
   - Кнопка открытия страницы активации

2. **Экран управления**
   - Красивые кнопки для каждого устройства
   - Информация об объекте
   - Статус "Онлайн"
   - Кнопки открывают `prime-skud.ru` в новой вкладке

### Инструкция для пользователя

```
1. Первый запуск:
   - Вставьте полученную ссылку в поле
   - Нажмите "Открыть страницу ворот"
   - На странице ПРАЙМ СКУД нажмите "Активировать на этом устройстве"
   - Куки сохранятся в браузере

2. Открытие ворот:
   - Нажмите "Использовать последнюю ссылку"
   - Выберите нужные ворота
   - На открывшейся странице нажмите кнопку открытия
   - Ворота откроются!

3. Повторное использование:
   - Куки уже сохранены
   - Просто нажмите на кнопку ворот
   - Ворота откроются
```

---

## 📁 Структура проекта

```
c:\Users\AntonSolo\Documents\AntiProject\Prime skud\
├── index.html          # Основная страница (готовое решение)
├── worker.js           # Cloudflare Workers (не используется)
└── README.md           # Этот файл
```

---

## 🚀 Размещение на GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите файл `index.html`
3. Settings → Pages → Branch: `main` → Save
4. Страница доступна по адресу:
   ```
   https://{username}.github.io/{repo}/
   ```

---

## 📱 Мобильная версия

Страница оптимизирована для мобильных устройств:

- Адаптивный дизайн
- PWA-совместимость
- Можно добавить на главный экран телефона
- Крупные кнопки для удобного нажатия

---

## 🔐 Безопасность

- Ссылка доступа сохраняется только в `localStorage` браузера
- Данные не передаются третьим сторонам
- Все запросы идут напрямую на `prime-skud.ru`

---

## ⚠️ Ограничения

1. **Требуется активация** — ссылка работает только на одном устройстве/браузере
2. **Открытие в новой вкладке** — невозможно обойти CORS без доступа к куки
3. **Куки браузера** — при очистке куки потребуется повторная активация

---

## 📊 Сравнение вариантов

| Вариант | Работает | CORS | Куки | Удобство |
|---------|----------|------|------|----------|
| Прямой fetch | ❌ | ❌ | ❌ | ⭐⭐⭐⭐⭐ |
| Fetch + credentials | ❌ | ❌ | ❌ | ⭐⭐⭐⭐⭐ |
| Cloudflare Workers | ⚠️ | ✅ | ❌ | ⭐⭐⭐⭐ |
| Форма + target="_blank" | ⚠️ | ✅ | ⚠️ | ⭐⭐⭐ |
| iframe | ❌ | ❌ | ❌ | ⭐⭐⭐⭐ |
| sendBeacon | ❌ | ❌ | ❌ | ⭐⭐⭐⭐ |
| **То же окно** | ✅ | ✅ | ✅ | ⭐⭐⭐⭐ |

---

## 📞 Контакты

Сервис предоставлен компанией **ПРАЙМ ПУЛЬТ**

- Сайт: https://prime-pult.ru/
- Система: ПРАЙМ СКУД

---

## 📝 Лицензия

Данный проект является пользовательской разработкой для личного использования.

🇷🇺 Сделано в России
