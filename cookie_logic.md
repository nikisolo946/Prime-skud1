# Логика работы с доступом ПРАЙМ СКУД

Данный документ описывает механизм авторизации, работы с хранилищем и логики кнопок интерфейса в приложении управления воротами (файл `index.html`).

## 1. Основные понятия

* **Ссылка доступа (URL)**: Уникальная ссылка вида `https://prime-skud.ru/a/1055423-4ed410458cc9b67`, которую генератор ПРАЙМ СКУД выдает пользователю. Сама по себе ссылка открывает страницу в браузере с кнопками ворот и кнопкой "Активировать" (при первом входе).
* **Хэш сессии (Куки / Cookie)**: Специальный токен авторизации (например, `bind_1055042=fcab1caf...`), который сервер выдает браузеру **после** нажатия кнопки "Активировать" на оригинальном сайте. Этот токен является вашим "электронным пропуском". Без него сервер не откроет ворота, даже если вы знаете верную ссылку.

В нашем приложении эти данные хранятся в постоянной памяти браузера устройства (Local Storage).

## 2. Логика хранения (Local Storage)
При загрузке страницы скрипт автоматически извлекает две переменные из памяти телефона/компьютера:
- `primeSkudUrl` (вставляется в поле "Ссылка доступа")
- `primeCookie` (вставляется в поле "Хэш сессии")

## 3. Логика кнопок управления доступом

В интерфейсе предусмотрены две разные кнопки для работы с этими данными, так как существуют разные сценарии использования:

### Кнопка "🚀 Активировать" (Сетевой запрос)
**Сценарий:** Вы впервые получили ссылку от администратора СКУД и вставили её в приложение.
**Как работает:**
1. Берет ссылку из верхнего поля.
2. Принудительно очищает текущий Хэш сессии (Куки) в памяти и в нижнем поле.
3. Отправляет скрытый запрос к серверу по указанной ссылке.
4. Если сервер сообщает, что ссылку нужно активировать, скрипт эмулирует нажатие кнопки "Активировать" на стороне сервера (отправляет POST-запрос `act_me=1`).
5. Сервер отвечает успешно и выдает **новую свежую Cookie**.
6. Cloudflare Worker перехватывает эту Cookie и передает нашему приложению.
7. Приложение сохраняет новую Cookie в Local Storage и подставляет её в поле "Хэш сессии".
8. Статус меняется на зеленый "Активировано".

### Кнопка "💾 Изменить ссылку" (Локальное сохранение)
**Сценарий:** Вы хотите дать доступ члену семьи (или перенести на другой телефон) без повторной активации на сервере.
**Как работает:**
1. Кнопка **НЕ делает никаких запросов** к серверу ПРАЙМ СКУД. 
2. Она просто берет текст, который вы руками вставили в поля "Ссылка" и "Хэш сессии".
3. Мгновенно сохраняет эти текстовые строчки в память браузера (Local Storage).
4. Таким образом, вы можете скопировать работающий длинный токен с одного телефона и вставить в другой, "клонируя" доступ. Статус сразу поменяется на "Активировано".

## 4. Логика открытия ворот и обработки ошибок

При нажатии на кнопку любых ворот (например, "Ворота 1") происходит следующее:
1. Приложение отправляет запрос к серверу на открытие, прикрепляя к нему сохраненный "Хэш сессии" (Куки).
2. Cloudflare Worker добавляет этот Хэш в настоящие заголовки `Cookie`, чтобы обмануть сервер ПРАЙМ СКУД (сервер думает, что запрос идет из обычного браузера на их сайте).

### Возможные ответы сервера (Логика статусов):
* **Успех (Ворота открыты)**: Сервер возвращает обычную страницу. Кнопка ворот зеленеет, статус остается "Активировано".
* **Ошибка (Ссылка удалена администратором)**: Сервер пишет русским языком `Ссылка удалена...`. Cloudflare Worker возвращает этот текст без нарушения кодировки. 
  * Приложение читает текст, зажигает красный индикатор "Ошибка: Ссылка удалена" и закрашивает кнопку красным. 
  * **Важно:** Сохраненный "Хэш" и Ссылка при этом **НЕ удаляются** из памяти по просьбе пользователя.
* **Ошибка (Активировано на другом устройстве)**: Сервер пишет `Уже активировано на другом устройстве...`. 
  * Приложение зажигает красный индикатор "Ошибка: Другое устройство".
  * "Хэш" также **НЕ удаляется** из памяти.
* **Сетевая ошибка**: Cloudflare не смог дозвониться до ПРАЙМ СКУД. Загорается "Ошибка сети".

Благодаря тому, что куки не сбрасываются автоматически при ошибках, пользователь всегда может попытаться починить доступ вручную через кнопку "Активировать" или вставив валидную куку и нажав "Изменить ссылку".
